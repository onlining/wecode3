from django.db import models

# class Posting(models.Model):
#     user=models.ForeignKey('signup.Signup',on_delete=models.CASCADE)
#     image= models.FileField(upload_to='media')
#     create=models.DateField(auto_now)

